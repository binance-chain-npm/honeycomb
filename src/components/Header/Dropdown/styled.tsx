import styled from 'styled-components';

import { headerItem } from '../styled';

export const Target = styled.div`
  ${headerItem};

  display: flex;
  align-items: center;
  white-space: nowrap;
`;
